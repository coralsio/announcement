<?php

namespace Tests\Feature;

use Corals\User\Models\User;
use Illuminate\Foundation\Testing\DatabaseTransactions;
use Illuminate\Support\Facades\Auth;
use Tests\TestCase;

class AnnouncementsViewTest extends TestCase
{
    use DatabaseTransactions;

    protected function setUp(): void
    {
        parent::setUp(); // TODO: Change the autogenerated stub

        $user = User::query()->whereHas('roles', function ($query) {
            $query->where('name', 'superuser');
        })->first();
        Auth::loginUsingId($user->id);
    }

    /**
     * A basic test example.
     *
     * @return void
     */
    public function test_announcements_view()
    {
        $response = $this->get('announcements');

        $response->assertStatus(200)->assertViewIs('Announcement::announcements.index');
    }

    public function test_announcements_create()
    {
        $response = $this->get('announcements/create');

        $response->assertViewIs('Announcement::announcements.create_edit')->assertStatus(200);
    }
}
